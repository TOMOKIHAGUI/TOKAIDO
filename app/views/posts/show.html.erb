<h2>投稿詳細</h2>
<div class="containar">
  <h3><%= @post.title %></h3>
  <table>
    <tbody id="post_<%= @post.id %>">
      <tr>
        <td>タグ</td>
        <% @post_tags.each do |tag| %>
          <td><%= link_to tag.tag_name, tag_posts_path(tag_id: tag.id) %></td>
        <% end %>
      </tr>
      <tr>
        <td><%= @post.prefecture %></td>
      </tr>
      <tr>
        <td>
          投稿者：<%= link_to "#{@post.user.name}", user_path(@post.user.id) %>
        </td>
      </tr>
      <tr>
        <%= render 'votes/vote-counts', post: @post %>
      </tr>
    </tbody>
  </table>
  <div><%= attachment_image_tag @post, :image, size: "200x240", format: 'jpg', fallback: "no_image.jpg", class: "default_image" %></div>
  <table>
    <tbody>
      <tr>
        <td>
          <%= @post.description %>
        </td>
      </tr>
      <tr>
        <td><%= @post.address %></td>
      </tr>
      <tr id="post_<%= @post.id %>">
        <td class="favorite-btn"><%= render 'favorites/favorite-btn', post: @post %></td>
      </tr>
    </tbody>
  </table>
  <table>
    <tbody id="post_<%= @post.id %>">
      <tr>
        <%= render 'votes/vote-btn', post: @post, vote: @vote %>
      </tr>
      <tr>
        <% if @post.user == current_user %>
          <td><%= link_to "編集する", edit_post_path(@post.id), class:"btn btn-sm btn-success" %></td>
          <td><%= link_to "削除する", post_path(@post.id), method: :delete, "data-confirm" => "本当に削除しますか？", class:"btn btn-sm btn-danger" %></td>
        <% else %>
          <td class="bookmark-btn"><%= render 'bookmarks/bookmark-btn', post: @post %></td>
        <% end %>
      </tr>
    </tbody>
  </table>
</div>
<div>
  <p>コメント数：<%= @post.post_comments.count %></p>
</div>

<div id = "comments">
  <%= render partial: 'post_comments/comment', locals: {post: @post} %>
</div>
<div class="new-comment">
  <%= render partial: 'post_comments/comment-form', locals: {post: @post, comment: @comment} %>
</div>

<script type="text/javascript">
  function initMap() {

    var test ={lat: <%= @post.latitude %>, lng: <%= @post.longitude %>};
    var map = new google.maps.Map(document.getElementById('map'), {
              zoom: 15,
              center: test
              });
    var transitLayer = new google.maps.TransitLayer();
    transitLayer.setMap(map);

    var contentString = '住所：<%= @post.address %>';
    var infowindow = new google.maps.InfoWindow({
      content: contentString
    });


    var marker = new google.maps.Marker({
                  position:test,
                  map: map,
                  title: contentString
                 });

     marker.addListener('click', function() {
       infowindow.open(map, marker);
     });
  }
</script>

<script async defer
              src="https://maps.googleapis.com/maps/api/js?v=3.exp&key=<%= ENV['GOOGLE_MAP_API_KEY'] %>&callback=initMap">
</script>
 <!-- 表示するマップの大きさ -->
<style type="text/css">
  #map { height: 200px;
         width: 70%;}
</style>

#mapの表示
<div id="map"></div>
<td>知らなかった！<%= post.votes.where(detail: 0).count %></td>
<td>聞いたことはある<%= post.votes.where(detail: 1).count %></td>
<td>行ったことある<%= post.votes.where(detail: 2).count %></td>
<td>有名でしょ！<%= post.votes.where(detail: 3).count %></td>
<% if current_user.following?(user) %>
  <%= link_to 'フォロー解除', unfollow_path(user.id), method: :DELETE, class: 'btn btn-info', remote: true %>
<% else %>
  <%= link_to 'フォローする', follow_path(user.id), method: :POST, class: 'btn btn-default', remote: true %>
<% end %>